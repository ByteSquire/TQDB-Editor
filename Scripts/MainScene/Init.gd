extends Node


# Called when the node enters the scene tree for the first time.
func _ready():
#	get_tree().root.gui_embed_subwindows = false
#	var basePosition = get_tree().root.position as Vector2
#	var window = Window.new()
#	window.position = basePosition + Vector2(40,40)
#	var mainWindow = load("res://Scenes/main.tscn").instantiate()
#	window.call_deferred("add_child", mainWindow)
#	get_tree().root.call_deferred("add_child", window)
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
